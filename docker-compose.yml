services:
  a1111:
    build: .
    container_name: a1111
    ports:
      - "7860:7860"
    volumes:
      - ./data:/workspace/data
    # add more volumes if you want to persist models, configs, etc.
    # e.g. - ./models:/workspace/stable-diffusion-webui/models
